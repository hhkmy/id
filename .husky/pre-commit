# Run lint-staged (handles Prettier, ESLint, etc.)
if [ "$OSTYPE" = "msys" ] || [ "$OSTYPE" = "win32" ]; then
  npx.cmd lint-staged
else
  npx lint-staged
fi

# Re-stage changes (if lint-staged modified files)
git add -u